<Window x:Class="snapwatch.DetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vwp="clr-namespace:WpfToolkit.Controls;assembly=VirtualizingWrapPanel"
        xmlns:vlc="clr-namespace:LibVLCSharp.WPF;assembly=LibVLCSharp.WPF"
        xmlns:local="clr-namespace:snapwatch"
        xmlns:components="clr-namespace:snapwatch.UI.Components"
        xmlns:controllers="clr-namespace:snapwatch.UI.Styles.Controllers"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d" Icon="pack://application:,,,/snapwatch.UI/Public/images/snapwatch-40px.ico"
        Title="snapwatch" Height="750" MinHeight="750" Width="934" MinWidth="934" Loaded="Window_Loaded">

    <Window.Resources>
        <controllers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Window.Resources>
    
    <Border>
        <Grid>
            <Image x:Name="BackgroundImage" Stretch="UniformToFill">
                <Image.Effect>
                    <BlurEffect Radius="30"/>
                </Image.Effect>
            </Image>

            <Rectangle Fill="#AA000000" />

            <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Orientation="Vertical" Margin="40">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20" HorizontalAlignment="Right">
                        <Border Margin="10,0">
                            <StackPanel MouseLeftButtonDown="ToHome_MouseLeftButtonDown">
                                <Image Source="pack://application:,,,/snapwatch.UI/Public/ico/layout-dashboard.png" Width="24" />
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Margin="0 0 40 0" Width="270" Height="375" CornerRadius="10">
                            <Border.Background>
                                <ImageBrush x:Name="PosterImageBrash" Stretch="UniformToFill" AlignmentX="Center" AlignmentY="Center">
                                    <ImageBrush.Transform>
                                        <ScaleTransform x:Name="scaleTransform" ScaleX="1" ScaleY="1" />
                                    </ImageBrush.Transform>
                                </ImageBrush>
                            </Border.Background>
                        </Border>

                        <StackPanel Grid.Column="1" VerticalAlignment="Top" Width="550">
                            <TextBox Text="{Binding TitleMovie}" Style="{StaticResource Font}" FontSize="36" FontWeight="Bold" Background="Transparent" BorderThickness="0" Foreground="White" MaxWidth="550" IsReadOnly="True" />
                            <TextBlock Text="{Binding GenreMovie}" FontSize="14" Foreground="Gray" Margin="0,4,0,16" />
                            <TextBox Text="{Binding DescriptionMovie}" TextWrapping="Wrap" Foreground="White" Background="Transparent" BorderThickness="0" FontSize="16" MaxWidth="600" IsReadOnly="True" />

                            <TextBlock Margin="0,20,0,0" Foreground="Gray" Text="Creators: Ivan Raimi, Sam Raimi, Tom Spezialy"/>
                            <TextBlock Foreground="Gray" Text="Stars: Bruce Campbell, Jill Marie Jones, Lucy Lawless"/>

                            <StackPanel Orientation="Horizontal" Margin="0,30,0,0" >
                                <Button Content="▶   ТРЕЙЛЕР" Width="150" Height="40" Margin="0,0,20,0" Tag="{Binding IdMovie}"
                                        Background="#FFFFC107" Foreground="Black" FontWeight="Bold" FontSize="13" 
                                        Style="{StaticResource Font}" Click="ShowTrailler_Click" />
                            </StackPanel>
                        </StackPanel>
                    </Grid>

                    <Label Content="Смотрите также" Style="{StaticResource Font}" Foreground="#fff" FontSize="23" Margin="0,20" />

                    <Grid MaxHeight="600">
                        <components:NotFound Visibility="{Binding HasNoMovies, Converter={StaticResource BoolToVisibilityConverter}}" />
                        
                        <ListBox ItemsSource="{Binding SimilarMovies}"
                                Background="Transparent"
                                BorderThickness="0"
                                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                PreviewMouseWheel="ListBox_PreviewMouseWheel"
                                ScrollViewer.CanContentScroll="True"
                                VirtualizingPanel.IsVirtualizing="True"
                                SelectionMode="Single" Height="Auto">

                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <vwp:VirtualizingWrapPanel IsItemsHost="True" Orientation="Horizontal" HorizontalAlignment="Center" />
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>

                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <components:MovieCardMini Title="{Binding Title}" PosterPath="{Binding PosterPath}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>

                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderBrush" Value="Transparent"/>
                                    <Setter Property="Focusable" Value="False"/>
                                    <Setter Property="OverridesDefaultStyle" Value="True"/>
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Margin" Value="0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <ContentPresenter />
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
